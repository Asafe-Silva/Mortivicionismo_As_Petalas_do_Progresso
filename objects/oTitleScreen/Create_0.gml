live_auto_call

// Menu Setup
menu_options = ["NOVO JOGO", "CONTINUAR", "CONFIGURAÇÕES", "CRÉDITOS"];
menu_selected = 0;

// Layout
gui_w = RES_W;
gui_h = RES_H;
display_set_gui_size(gui_w, gui_h);

// Animation / Juice
cursor_offset = 0;
cursor_dir = 1;
time = 0;
